// Stream API key — public, safe to commit
export const STREAM_API_KEY = '5azqrwjz9fra';

// Fly.io backend — token minting + calendar proxy
export const BACKEND_URL = 'https://yap-calendar-backend.fly.dev';

// Both identities the app supports
export const STREAM_USERS = {
  kath:   { id: 'kath',   name: 'Kath' },
  adrian: { id: 'adrian', name: 'Adrian' },
};
